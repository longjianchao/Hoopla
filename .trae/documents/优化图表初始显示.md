1. 修改`drawChiSquareCurve`函数，使其在数据为空时也能创建完整的图表框架
2. 修改`drawParamsTrendChart`函数，使其在数据为空时也能为每个参数创建图表框架
3. 确保页面加载时初始化这些图表，显示完整的轴线和标题
4. 检查`cleanupCanvas`函数，确保清理后重新绘制带有轴线和标题的空图表

具体修改点：

* 在`drawChiSquareCurve`中，当`chiSquaredValues`为空时，创建带有默认配置的图表

* 在`drawParamsTrendChart`中，当`paramsHistory`为空时，为每个参数创建带有默认配置的图表

* 确保图表的scales、title等配置在空数据时仍然显示

* 调整图表初始化逻辑，确保页面加载时就显示完整的图表框架

